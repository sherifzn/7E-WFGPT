# Dependency Inventory

Generated by `node scripts/dependency-inventory.mjs`.

## Runtime Dependencies

| Module | Dependency | Purpose | License | Security impact | Operational impact |
|---|---|---|---|---|---|
| frontend | `@vitejs/plugin-react` | React integration for Vite | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `react` | Frontend UI runtime | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `react-dom` | Browser rendering for React | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `typescript` | Type checking for frontend contracts | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `vite` | Frontend build and local development server | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |

## Development and Test Dependencies

| Module | Dependency | Purpose | License | Security impact | Operational impact |
|---|---|---|---|---|---|
| root | `prettier` | Repository formatting checks | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `@eslint/js` | JavaScript linting rules | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `@testing-library/jest-dom` | DOM assertions for frontend tests | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `@testing-library/react` | React component test helpers | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `@types/react` | React TypeScript types | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `@types/react-dom` | React DOM TypeScript types | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `eslint` | Frontend linting | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `globals` | Browser global definitions for linting | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `jsdom` | DOM runtime for component tests | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `typescript-eslint` | TypeScript linting support | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| frontend | `vitest` | Frontend unit-test framework | See package metadata | Limited to local development/CI supply chain risk | Installed by npm for local and CI checks |
| Maven | `org.junit:junit-bom` / `org.junit.jupiter:junit-jupiter` | Java unit-test framework | EPL-2.0 | Test-only supply chain risk | Used by Maven test phase |
| Maven | `com.tngtech.archunit:archunit-junit5` | Architecture dependency tests | Apache-2.0 | Test-only supply chain risk | Used by Maven test phase |
| Maven plugin | `com.diffplug.spotless:spotless-maven-plugin` | Java and POM formatting checks | Apache-2.0 | Build-plugin supply chain risk | Used by Maven format checks |
| Maven plugin | `org.apache.maven.plugins:maven-dependency-plugin` | Java dependency resolution and tree scanning | Apache-2.0 | Build-plugin supply chain risk | Used by dependency scan checks |
| GitHub Action | `actions/dependency-review-action` | Pull-request dependency vulnerability review | MIT | CI supply chain risk | Runs only in pull-request checks |
| Internal module | `contracts -> domain` | Reuses the canonical domain data-classification model in transport-facing contracts | Internal project code | No third-party supply chain impact | Built in the Maven reactor |
| Internal module | `adapters -> domain` | Keeps serialization schema access separate from pure domain contracts | Internal project code | No third-party supply chain impact | Built in the Maven reactor |
| Internal module | `architecture-tests -> domain/adapters/backend/contracts` | Verifies module dependency boundaries and forbidden integration markers | Internal project code | No third-party supply chain impact | Used by Maven test phase |
